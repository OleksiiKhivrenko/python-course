# settings - настройки проекта
# root url config
# asgi wsgi - точки входа (код в этих файлах первым обрабатывает запрос)
# asgi - async

# templates - принимает шаблон (html) и контекст (объект в котором хранятся данные)
